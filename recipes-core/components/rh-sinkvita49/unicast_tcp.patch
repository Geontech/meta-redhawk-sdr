diff --git cpp/unicast_tcp.cpp cpp/unicast_tcp.cpp
index 8f1f410..04ae600 100644
--- cpp/unicast_tcp.cpp
+++ cpp/unicast_tcp.cpp
@@ -37,7 +37,7 @@ static inline void verify_ (int condition, const char* message, const char* cond
     if (!condition) {
         char msg[100];
         sprintf(msg, "Verify failed '%s' at line %d: %s (%s)\n", file, line, message, condtext);
-        fprintf(stderr, msg);
+        fprintf(stderr,"%s", msg);
         perror("perror");
         throw(BadParameterError4(msg));
     }
